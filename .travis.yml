language: python
python:
   - "3.5"
before_install:
   - curl -L -o lorax_tool.sh https://raw.githubusercontent.com/LegumeFederation/lorax/master/build_scripts/lorax_tool.sh
   - chmod 755 lorax_tool.sh
   - ./lorax_tool.sh download
   - ./lorax_tool.sh init
   - export root_dir=`python -c 'import sys;print(sys.prefix)'`
   - ./lorax_tool.sh config root_dir $root_dir
   - ./lorax_tool.sh config var_dir ${root_dir}/var
   - ./lorax_tool.sh config tmp_dir ${root_dir}/var/tmp
   - ./lorax_tool.sh config log_dir ${root_dir}/var/log
   - ./lorax_tool.sh config python system
   - ./lorax_tool.sh config raxml system
   - ./lorax_tool.sh config redis system
   - ./lorax_tool.sh config all
   - ./lorax_tool.sh make_dirs
   - ./lorax_tool.sh install
install:
   - pip install -U setuptools
   - pip install -e 'git+https://github.com/LegumeFederation/supervisor.git@4.0.0#egg=supervisor==4.0.0'
   - pip install .
   - pip install coveralls
before_script:
   - lorax_env lorax config secret_key
   - lorax_env lorax config secret_key blah
   - lorax_env lorax config
   - lorax_env lorax create_instance
   - lorax_env lorax create_test_files
   - lorax_env lorax set_htpasswd
   - lorax_env lorax test_logging
script:
   - export LORAX_COVERAGE=1
   - lorax_env supervisord
   - while lorax_env supervisorctl status | grep STARTING >/dev/null; do sleep 5; done
   - lorax_env supervisorctl status
   - lorax_env lorax rq info
   - ./test_targets.sh
   - lorax_env supervisorctl stop lorax
   - lorax_env supervisorctl shutdown
after_success:
   - coveralls
